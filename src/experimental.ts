/**
 * Experimental entrypoint.
 * Consumers can opt into previewed contracts while isolating them from the stable surface.
 */
export const stability = 'experimental' as const;

export * from './index';
